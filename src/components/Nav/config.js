import React from 'react';

const Config = () => {
  // return <div>
  // region: clickedCity,
  // check_in: checkInDate,
  // check_out: checkOutDate,
  // max_guest: adultCount + ChildrenCount + infantCount,
  // max_pet: petCount</div>;
};

export default Config;
